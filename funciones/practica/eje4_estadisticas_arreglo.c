/*
 * =============================================================================
 * Archivo     : eje4_estadisticas_arreglo.c
 * Modulo      : Funciones > Practica
 * Ejercicio   : 4 de 5
 * Descripcion : Calculo de estadisticas sobre un arreglo usando funciones
 *               especializadas que reciben el arreglo y su tamano.
 *
 * Instrucciones:
 *   1. Declare e implemente las siguientes funciones:
 *      - `void llenarArreglo(int arr[], int n)`: Lee n enteros del usuario y
 *        los guarda en el arreglo.
 *      - `void mostrarArreglo(int arr[], int n)`: Muestra el arreglo en una linea.
 *      - `double calcularPromedio(int arr[], int n)`: Retorna el promedio como real.
 *      - `int encontrarMaximo(int arr[], int n)`: Retorna el valor maximo del arreglo.
 *      - `int encontrarMinimo(int arr[], int n)`: Retorna el valor minimo del arreglo.
 *   2. En main():
 *      - Pida la cantidad de elementos y declare el VLA.
 *      - Llame a llenarArreglo().
 *      - Llame a mostrarArreglo().
 *      - Imprima los resultados de las funciones de calculo (promedio, max, min).
 *
 * Compilar : gcc -Wall -std=c99 eje4_estadisticas_arreglo.c -o eje4
 * Ejecutar : ./eje4
 * =============================================================================
 */

#include <stdio.h>

// TODO: 1. Declarar los prototipos de funciones.

int main()
{
    // TODO: 2. Leer tamano, declarar el VLA y orquestar las llamadas a las funciones.

    return 0;
}

// TODO: 3. Definir llenarArreglo() y mostrarArreglo().

// TODO: 4. Definir calcularPromedio().

// TODO: 5. Definir encontrarMaximo() y encontrarMinimo().
