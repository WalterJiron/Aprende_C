/*
 * =============================================================================
 * Archivo     : eje5_tabla2d.c
 * Modulo      : Arreglos > Practica
 * Ejercicio   : 5 de 5
 * Descripcion : Generacion y visualizacion de la tabla de multiplicar usando
 *               un arreglo bidimensional (matriz) de enteros.
 *
 * Instrucciones:
 *   1. Pida al usuario un entero N (limite de la tabla, valide entre 1 y 20).
 *   2. Declare una matriz de enteros de N x N (VLA bidimensional).
 *   3. Llene la matriz de forma que matriz[i][j] guarde el resultado de (i+1)*(j+1).
 *   4. Imprima la matriz de forma alineada utilizando printf con formato
 *      adecuado (por ejemplo, `%4d` para alinear las columnas).
 *   5. Imprima etiquetas para las filas y columnas para facilitar la lectura.
 *
 * Compilar : gcc -Wall -std=c99 eje5_tabla2d.c -o eje5
 * Ejecutar : ./eje5
 * =============================================================================
 */

#include <stdio.h>

int main()
{
    // TODO: 1. Solicitar N (limite de la tabla) validando que este entre 1 y 20.

    // TODO: 2. Declarar la matriz bidimensional VLA de N x N.

    // TODO: 3. Llenar la matriz con la tabla de multiplicar (i+1)*(j+1) usando bucles anidados.

    // TODO: 4. Imprimir el encabezado de las columnas.

    // TODO: 5. Imprimir la matriz completa con etiquetas de fila a la izquierda.

    return 0;
}
