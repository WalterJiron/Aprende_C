/*
 * =============================================================================
 * Archivo     : eje2_conversion_temperatura.c
 * Modulo      : Funciones > Practica
 * Ejercicio   : 2 de 5
 * Descripcion : Conversor de temperatura con menu, utilizando funciones puras.
 *
 * Instrucciones:
 *   1. Declare e implemente las siguientes funciones de conversion:
 *      - double celsiusAFahrenheit(double celsius);
 *      - double fahrenheitACelsius(double fahrenheit);
 *      - double celsiusAKelvin(double celsius);
 *      - double kelvinACelsius(double kelvin);
 *   2. Implemente una funcion de lectura para leer y validar numeros reales (double).
 *   3. En main(), construya un menu iterativo con las 4 opciones de conversion y 0 para salir.
 *   4. Solicite la temperatura de entrada, llame a la funcion correspondiente e
 *      imprima el resultado directamente (ej: `printf("%.2f C = %.2f F", temp, celsiusAFahrenheit(temp));`).
 *
 * Formulas:
 *   F = C * 9/5 + 32
 *   C = (F - 32) * 5/9
 *   K = C + 273.15
 *   C = K - 273.15
 *
 * Compilar : gcc -Wall -std=c99 eje2_conversion_temperatura.c -o eje2
 * Ejecutar : ./eje2
 * =============================================================================
 */

#include <stdio.h>

// TODO: 1. Declarar prototipos de las funciones de conversion y lectura.

int main()
{
    // TODO: 2. Implementar bucle del menu de conversion llamando a las funciones.

    return 0;
}

// TODO: 3. Implementar cada una de las funciones de conversion.

// TODO: 4. Implementar la funcion de lectura validada para doubles.
