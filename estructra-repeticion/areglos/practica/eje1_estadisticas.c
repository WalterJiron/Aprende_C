/*
 * =============================================================================
 * Archivo     : eje1_estadisticas.c
 * Modulo      : Arreglos > Practica
 * Ejercicio   : 1 de 5
 * Descripcion : Calculo de estadisticas basicas sobre un arreglo de enteros:
 *               mayor, menor, promedio y suma total.
 *
 * Instrucciones:
 *   1. Solicite al usuario la cantidad de numeros a ingresar (valide que sea > 0).
 *   2. Declare un arreglo de longitud variable (VLA) con el tamano ingresado.
 *   3. Solicite al usuario ingresar cada uno de los numeros enteros y guardelos en el arreglo.
 *   4. Recorra el arreglo para encontrar el valor maximo, el minimo, la suma y
 *      calcule el promedio.
 *   5. Muestre los resultados obtenidos en pantalla.
 *
 * Compilar : gcc -Wall -std=c99 eje1_estadisticas.c -o eje1
 * Ejecutar : ./eje1
 * =============================================================================
 */

#include <stdio.h>

int main()
{
    // TODO: 1. Declarar variables para el tamano del arreglo e ingresar el valor validando que sea > 0.

    // TODO: 2. Declarar el arreglo (VLA).

    // TODO: 3. Llenar el arreglo pidiendo los datos al usuario.

    // TODO: 4. Inicializar y calcular: mayor, menor, suma y promedio.
    // Pista: inicialice 'mayor' y 'menor' con el primer elemento del arreglo para evitar valores magicos.

    // TODO: 5. Mostrar los resultados (mayor, menor, suma y promedio con dos decimales).

    return 0;
}
