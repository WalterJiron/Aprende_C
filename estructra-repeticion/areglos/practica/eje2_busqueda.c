/*
 * =============================================================================
 * Archivo     : eje2_busqueda.c
 * Modulo      : Arreglos > Practica
 * Ejercicio   : 2 de 5
 * Descripcion : Busqueda lineal (secuencial) en un arreglo de enteros.
 *               El usuario carga el arreglo y luego ingresa un numero para
 *               buscar. El programa indica si el numero existe y en que
 *               posicion(es) se encuentra.
 *
 * Instrucciones:
 *   1. Solicite al usuario la cantidad de numeros a ingresar y declare el VLA.
 *   2. Llene el arreglo con la entrada del usuario.
 *   3. Solicite el numero entero que se desea buscar.
 *   4. Implemente el algoritmo de busqueda lineal: recorra el arreglo y, si
 *      encuentra el numero, muestre su posicion (base 1 para el usuario).
 *   5. Muestre la cantidad total de ocurrencias encontradas. Si no existe,
 *      indiquelo con un mensaje descriptivo.
 *
 * Compilar : gcc -Wall -std=c99 eje2_busqueda.c -o eje2
 * Ejecutar : ./eje2
 * =============================================================================
 */

#include <stdio.h>

int main()
{
    // TODO: 1. Leer cantidad de elementos y declarar el arreglo.

    // TODO: 2. Llenar el arreglo con numeros enteros.

    // TODO: 3. Solicitar el numero entero a buscar (objetivo).

    // TODO: 4. Realizar la busqueda lineal recorriendo el arreglo.
    // Lleve un control de si se encontro al menos una vez (bandera)
    // y cuantas veces aparece (contador de ocurrencias).

    // TODO: 5. Mostrar resultados al usuario indicando las posiciones base 1 o si no se encontro.

    return 0;
}
